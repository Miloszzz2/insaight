import { NextResponse } from "next/server"
export async function GET(request: Request) {

    //const data = await fetch(`https://www.googleapis.com/youtube/v3/channels?part=contentDetails&forUsername=MrBeast&key=${process.env.YOUTUBE_API_KEY}`).catch((error) => NextResponse.json(error))

    //const info = await data.json();

    //    return NextResponse.json(info)
}